import Carousal from "./Carousal";

let Home = () => {
  return (
    <div>
      Home Component
      <Carousal></Carousal>


      {/* <div class="row">
        {cakeData.map((ele, index) => {
          return <Cake data={ele} key={index}></Cake>;
        })}
      </div> */}
    </div>
  );
};

export default Home;
